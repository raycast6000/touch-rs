# Usage
It's pretty much the same as the original one.
```bash
touch File1 File2 path/to/File3 ...
```
# Installation
Run this command in your terminal.
```bash
cargo install --git https://github.com/raycast6000/touch.git
```